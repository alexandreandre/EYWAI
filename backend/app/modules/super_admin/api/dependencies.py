"""
Dépendances API du module super_admin.

La dépendance verify_super_admin (guard super admin) est définie dans router.py
pour éviter les imports circulaires avec app.core.security et le service applicatif.
"""
