# Domain entities for rates.
#
# Ce module est en lecture seule (configs payroll). Pas d'entité métier riche pour l'instant :
# la sortie est un dictionnaire de projection (config_key -> métadonnées + config_data).
# Conserver ce fichier comme placeholder pour évolution future (ex. valeur objet RateCategoryEntry).
