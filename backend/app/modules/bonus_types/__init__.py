# Module bonus_types — migration terminée.
# Router : app.modules.bonus_types.api.router (branché dans app.api.router).
# Legacy : api.routers.bonus_types reste monté dans main.py pour compatibilité.
