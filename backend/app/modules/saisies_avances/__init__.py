# Module saisies_avances — migré.
# Nouveau router : branché dans app.api.router (app.modules.saisies_avances.api.router).
# Legacy : api.routers.saisies_avances reste dans main.py pour compatibilité (point d'entrée ancien).
