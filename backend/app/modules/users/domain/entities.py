"""
Entités du domaine users.

User : à aligner avec schemas.user.User (id, email, first_name, last_name,
is_super_admin, accessible_companies, active_company_id, propriétés calculées
role/company_id et méthodes has_access_to_company, get_role_in_company,
is_admin_in_company, has_rh_access_in_company, etc.).
"""

# Placeholder : l'entité User sera migrée ou mappée depuis schemas.user.User.
# Le domain ne dépend pas de Pydantic ; on pourra avoir une entité pure + mapper vers le schéma API.
