"""
Schémas de requête du module residence_permits.

Aucun schéma pour l’instant : la seule route exposée est GET /api/residence-permits (liste, pas de body).
Réservé pour évolution (ex. filtres ou paramètres de requête typés).
"""
