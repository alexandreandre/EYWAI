export const CHART_PYRAMID_COLORS = [
  "hsl(var(--primary))",
  "hsl(var(--primary) / 0.88)",
  "hsl(var(--primary) / 0.76)",
  "hsl(var(--primary) / 0.64)",
  "hsl(var(--primary) / 0.52)",
  "hsl(var(--primary) / 0.4)",
];

export const CHART_CONTRACT_COLORS = [
  "hsl(var(--primary))",
  "hsl(var(--primary) / 0.75)",
  "hsl(var(--primary) / 0.55)",
  "hsl(var(--primary) / 0.4)",
  "hsl(var(--muted-foreground) / 0.5)",
];

export const ABSENCE_COLORS = {
  maladie: "hsl(var(--primary))",
  at: "hsl(var(--primary) / 0.6)",
  autres: "hsl(var(--muted-foreground) / 0.45)",
};
