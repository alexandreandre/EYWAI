/**
 * Feature flags produit (UI uniquement).
 * Le backend et les APIs restent actifs — réactiver ici pour afficher à nouveau.
 */

/** Alertes RIB (modification / doublon) sur dashboard, liste salariés, sidebar. */
export const RIB_ALERTS_UI_ENABLED = false;
